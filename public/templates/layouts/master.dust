<!DOCTYPE html>
<html class="no-js">
<head>
  <meta charset="utf-8">
  <meta http-equiv="X-UA-Compatible" content="IE=edge">
  <title>{+title /}</title>
  <meta name="description" content="">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <link rel="stylesheet" href="style.css" />
</head>
<body>

<div id="wrapper">
  {+body /}
</div>

<script src="js/lib/socketio.js"></script>
<script>
  var currentTree = 1;
  var percentSize = 0;
  var leavesPerTree = 100;
  var treeSizeOriginal = [300, 250, 200, 300, 150];

  var socket = io.connect('/');
  socket.on('ping', function (data) {
    treeSize = treeSizeOriginal[currentTree-1]*(data/leavesPerTree) + 'px';
    // document.getElementById('t' + currentTree).style.width = treeSize;
    // document.getElementById('t' + currentTree).style.height = treeSize;
    if(data === 0) currentTree += 1;
  });
</script>

</body>
</html>
